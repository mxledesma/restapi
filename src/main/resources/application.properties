spring.application.name=restapi

# Database properties
spring.datasource.url=jdbc:mysql://localhost:3306/expensetransactions

spring.datasource.username=root

#If MySQL installation is password proctored,then use below property to set password
spring.datasource.password=LearLanceJuliette1

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# I needed this to udpate the Database
spring.jpa.hibernate.ddl-auto=update

# To see table structure
# USE expensetransactions;
#DESCRIBE tbl_expenses;
#Select * from tbl_expenses;

# INSERT INTO tbl_expenses
# (expense_id, name, note, category, amount, date, created_at, updated_at)
# VALUES
# (uuid(), 'Electricity bill', 'electricity bill', 'Utilities', 500.00, curdate(), utc_timestamp(), utc_timestamp()),
# (uuid(), 'Water bill', 'water bill', 'Utilities', 1000.00, curdate(), utc_timestamp(), utc_timestamp());

# TO check the api  note the api is /api/vi look at line 34
#http://localhost:8765/api/vi/expenses

server.port=8765

# Content added new
#server.servlet.context-path=/api/vi